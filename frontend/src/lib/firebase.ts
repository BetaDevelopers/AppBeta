/**
 * firebase.ts — stub
 * L'app utilitza Supabase com a backend d'autenticació.
 * Firebase no és una dependència activa; aquest fitxer existeix per compatibilitat
 * amb imports antics i no exporta res funcional.
 */

export const auth = null;
export const firestore = null;
export const storage = null;
